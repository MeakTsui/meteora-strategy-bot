# ============================================================================
# Meteora DLMM Bot 配置文件
# 复制此文件为 .env 并填入实际值
# ============================================================================

# ----------------------------------------------------------------------------
# 钱包配置（推荐使用加密密钥文件）
# ----------------------------------------------------------------------------
# 方式1（推荐）: 加密密钥文件路径
# 使用 npx ts-node src/scripts/encryptKey.ts 生成加密文件
# 启动时需要输入密码解密
ENCRYPTED_KEY_PATH=./encrypted_key.json

# 方式2: 私钥（Base58 编码）- 不推荐用于生产环境
# WALLET_PRIVATE_KEY=your_base58_private_key_here

# 方式3: 助记词（12或24个单词，空格分隔）- 不推荐用于生产环境
# WALLET_MNEMONIC=word1 word2 word3 word4 word5 word6 word7 word8 word9 word10 word11 word12

# ----------------------------------------------------------------------------
# RPC 配置
# ----------------------------------------------------------------------------
# Solana RPC 节点地址（建议使用付费节点以获得更好的性能）
RPC_URL=https://api.mainnet-beta.solana.com

# ----------------------------------------------------------------------------
# Bid-Ask Rebalancer 配置
# ----------------------------------------------------------------------------
# 监控间隔（毫秒），默认 30000（30秒）
MONITOR_INTERVAL_MS=30000

# 优先费（microLamports），默认 1000，设置较低以节省 gas
PRIORITY_FEE=1000

# 是否启用详细日志，默认 false
VERBOSE=false

# ----------------------------------------------------------------------------
# Dashboard 配置
# ----------------------------------------------------------------------------
# Dashboard 服务端口，默认 3000
DASHBOARD_PORT=3000

# ----------------------------------------------------------------------------
# 自动领取手续费配置
# ----------------------------------------------------------------------------
# 是否启用自动领取手续费，默认 true
CLAIM_FEE_ENABLED=true

# 触发领取的最低 USD 阈值（所有仓位总和），默认 5
CLAIM_FEE_THRESHOLD_USD=5

# 单个仓位手续费最小领取阈值（USD），低于此值不领取，节省 gas，默认 0.1
CLAIM_FEE_MIN_POSITION_USD=0.1

# 每天检查时间（小时，0-23），默认 8 点
CLAIM_FEE_CHECK_HOUR=8
